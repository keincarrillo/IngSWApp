<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos para notificaciones -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BROADCAST" />
    <!-- Permisos para podometro -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <!-- Permisos para leer archivos -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />



    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Koalm"
        tools:targetApi="31">


        <!-- Servicio del podómetro -->
        <service
            android:name=".services.MovimientoService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="health" />

        <!-- Servicio para manejar las notificaciones -->
        <service
            android:name=".services.timers.NotificationService"
            android:enabled="true"
            android:exported="false" />

        <!-- Servicio para manejar el temporizador -->
        <service
            android:name=".services.timers.WritingTimerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- Servicio para manejar el temporizador de lectura -->
        <service
            android:name=".services.timers.ReadingTimerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- Servicio para manejar el temporizador de meditación -->
        <service
            android:name=".services.timers.MeditationTimerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- Receptor para las notificaciones programadas -->
        <receiver
            android:name=".services.timers.NotificationReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.koalm.NOTIFICATION_ACTION" />
                <action android:name="com.example.koalm.START_TIMER_ACTION" />
            </intent-filter>
        </receiver>

        <!-- Receptor para las actualizaciones del temporizador -->
        <receiver
            android:name=".services.TimerUpdateReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.koalm.TIMER_UPDATE_ACTION" />
            </intent-filter>
        </receiver>

        <!-- Servicio para manejar el temporizador de desconexión digital -->
        <service
            android:name=".services.timers.DigitalDisconnectTimerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- NOTpers-->
        <receiver
            android:name=".services.timers.NotificationReceiverPers"
            android:enabled="true"
            android:exported="true"
        />



        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize"  android:theme="@style/Theme.Koalm">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>